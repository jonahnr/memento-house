export const PHYSICAL_STATES=["payment_pending","paid","awaiting_customer_information","ready_for_design","design_in_progress","proof_ready","awaiting_customer_approval","changes_requested","approved","in_production","ready_to_ship","shipped","delivered","completed","cancelled","refunded"] as const;
export const DIGITAL_STATES=["payment_pending","paid","account_ready","active","cancelled","refunded"] as const;
export type OrderState=(typeof PHYSICAL_STATES)[number]|(typeof DIGITAL_STATES)[number];
const transitions:Record<string,string[]>={payment_pending:["paid","cancelled"],paid:["awaiting_customer_information","ready_for_design","account_ready","refunded"],awaiting_customer_information:["ready_for_design","cancelled","refunded"],ready_for_design:["design_in_progress","cancelled","refunded"],design_in_progress:["proof_ready","cancelled","refunded"],proof_ready:["awaiting_customer_approval","cancelled","refunded"],awaiting_customer_approval:["changes_requested","approved","cancelled","refunded"],changes_requested:["design_in_progress","cancelled","refunded"],approved:["in_production","cancelled","refunded"],in_production:["ready_to_ship","cancelled","refunded"],ready_to_ship:["shipped","cancelled","refunded"],shipped:["delivered","refunded"],delivered:["completed","refunded"],account_ready:["active","refunded"],active:["refunded"],completed:["refunded"],cancelled:[],refunded:[]};
export function canTransition(from:string,to:string){return transitions[from]?.includes(to)??false}
export function initialPaidState(workflow:"digital_map"|"custom_print",needsQuestionnaire=true){return workflow==="digital_map"?"account_ready":needsQuestionnaire?"awaiting_customer_information":"ready_for_design"}
export function assertTransition(from:string,to:string){if(!canTransition(from,to))throw new Error(`Invalid order transition: ${from} → ${to}`)}
