import {createClient,type SupabaseClient,type User} from "@supabase/supabase-js";
import {supabaseServerConfig} from "./server-config";
export async function requireUser(request:Request):Promise<{admin:SupabaseClient;user:User}|null>{const{url,serviceRoleKey:key}=supabaseServerConfig(),token=(request.headers.get("authorization")||"").replace(/^Bearer\s+/i,"");if(!key||!token)return null;const admin=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}});let auth;try{auth=await Promise.race([admin.auth.getUser(token),new Promise<never>((_,reject)=>setTimeout(()=>reject(new Error("Account verification timed out")),6000))])}catch(error){console.error("Account verification failed before checkout",error);return null}return auth.data.user?{admin,user:auth.data.user}:null}
