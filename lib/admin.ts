import {createClient,type SupabaseClient, type User} from "@supabase/supabase-js";
export const ADMIN_EMAILS=(process.env.ADMIN_EMAILS||"jonahnr@gmail.com").toLowerCase().split(",").map(x=>x.trim()).filter(Boolean);
export async function requireAdmin(request:Request):Promise<{admin:SupabaseClient;user:User}|null>{const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY,token=(request.headers.get("authorization")||"").replace(/^Bearer\s+/i,"");if(!url||!key||!token)return null;const admin=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}}),auth=await admin.auth.getUser(token),user=auth.data.user;if(!user||!ADMIN_EMAILS.includes(user.email?.toLowerCase()||""))return null;return{admin,user}}
export async function adminFailure(request:Request){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY,token=(request.headers.get("authorization")||"").replace(/^Bearer\s+/i,"");if(!url||!key)return Response.json({error:"Administrator services are not configured in this Vercel environment. Add NEXT_PUBLIC_SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY, then redeploy."},{status:503});if(!token)return Response.json({error:"Your login session was not included. Sign out, sign back in, and retry."},{status:401});const admin=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}}),auth=await admin.auth.getUser(token);if(auth.error||!auth.data.user)return Response.json({error:`Supabase rejected this login session: ${auth.error?.message||"user not found"}`},{status:401});return Response.json({error:`${auth.data.user.email||"This account"} is signed in but is not in ADMIN_EMAILS for this Vercel environment.`},{status:403})}
