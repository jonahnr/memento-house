# Address coverage and QR print verification — 2026-09-07

## Address diagnosis

The live Photon request for `1050 Chancellors Dr, Statham, GA 30666` returned HTTP 200 with an empty `features` array. The Census Current Address Ranges endpoint matched `1050 CHANCELLORS DR, STATHAM, GA, 30666` at latitude `33.941078891457`, longitude `-83.590470383294` (TIGER line `611733549`, side L).

The search now appends Census matches for numbered US street queries while retaining every original Photon result and its ordering. Either provider can fail independently. No migration, deletion, rewrite, or customer-data backfill is performed. Census coordinates are street-range estimates, explicitly labeled in the result picker, rather than a promise of rooftop precision. Provider documentation: https://geocoding.geo.census.gov/geocoder/Geocoding_Services_API.html

`node tools/verify-address-search.mjs` exercised the real search route, selected the reported address, and verified coordinates in a mocked milestone insert, preserving an existing Paris milestone. All persistence was intercepted; no customer data changed.

## Print checks

Classic Gold, Garden Arch, and Modern Editorial were verified in all four layouts: letter, two cards on letter, 4×6, and 5×7. All 12 exported PDFs have exactly one page and correct physical page boxes. Both Map Plus and Timeline Plus invitation wording was checked across all combinations. Mobile previews passed at 390 px, with no horizontal overflow. No print content overflowed card bounds.

The existing brand logo's 1254×1254 source was compressed to WebP (122,068 bytes), without upscaling. The clean QR remains 960×960 with a four-module quiet zone and high error correction. Decorative shapes and text use CSS/vector printing, with no enlarged bitmap watermark.

Minimum source-image effective PPI at print size: 355.57. Minimum embedded PDF image PPI, calculated from each image's pixel dimensions and composed PDF transformation matrix: 300.00001. Chrome rasterizes the logo's visual effect at 300 PPI; the QR retains all 960 pixels. All rendered PDF QR codes, including both copies on double sheets, decode to the correct map URL. Print at the selected paper size and 100% scale, with headers/footers off.

Checks: 40 unit/regression tests passed; production build passed; scoped ESLint passed. Browser checks: `tools/verify-qr-formats.mjs`. PDF dimensions/resolution: `tools/inspect-qr-pdfs.py` (pypdf). Actual PDF rendering/scanning: `tools/render-qr-pdfs.mjs` (bundled pdfjs-dist/canvas, jsqr). Generated previews/PDFs remain local and can be regenerated by these scripts.

## Partial-address correction

The follow-up screenshot showed `1050 chancellors dr statha`, without a state or ZIP. The previous Census gate excluded this input even though a direct live Census request matches it correctly. Removed the state/ZIP requirement for numbered street queries. Existing Photon suggestions remain in their original order, with Census matches appended. Updated regression coverage to use the exact screenshot fragment and preserve international results. Unit tests: 42 passed; production build passed.
