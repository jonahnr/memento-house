import {LoginForm} from "../auth-form";export const metadata={title:"Account login — Memento House"};export default async function Login({searchParams}:{searchParams:Promise<{return_to?:string}>}){const raw=(await searchParams).return_to||"/account",returnTo=raw.startsWith("/")&&!raw.startsWith("//")?raw:"/account";return <main className="auth"><a href="/" className="brand"><img src="/brand/memento-house-logo.webp" alt="Memento House"/><span>Memento House</span></a><LoginForm returnTo={returnTo}/><aside className="newAccountPrompt"><div><b>New to Memento House?</b><span>Create one account for your orders, customizations, proofs, shipping, and digital keepsakes.</span></div><a className="button light" href={`/signup?return_to=${encodeURIComponent(returnTo)}`}>Create an account →</a></aside></main>}
