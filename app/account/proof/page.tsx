"use client";
import {useEffect,useState} from "react";
import {getSupabaseBrowserClient} from "../../../lib/supabase";
export default function ProofReview(){const[id,setId]=useState(""),[data,setData]=useState<any>(),[feedback,setFeedback]=useState(""),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);async function accessToken(){return(await getSupabaseBrowserClient()?.auth.getSession())?.data.session?.access_token}useEffect(()=>{const proofId=new URLSearchParams(location.search).get("id")||"";setId(proofId);(async()=>{try{const token=await accessToken();if(!token){location.href=`/login?return_to=${encodeURIComponent(location.pathname+location.search)}`;return}const response=await fetch(`/api/account/proofs?id=${encodeURIComponent(proofId)}`,{headers:{authorization:`Bearer ${token}`}}),result=await response.json().catch(()=>({}));if(!response.ok)throw new Error(result.error||"The proof could not be loaded.");setData(result);setFeedback(result.proof.customer_feedback||"")}catch(error){setMessage((error as Error).message)}})()},[]);async function respond(decision:"approved"|"changes_requested"){setBusy(true);setMessage("");try{const token=await accessToken(),response=await fetch("/api/proof-response",{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${token}`},body:JSON.stringify({proofId:id,decision,feedback})}),result=await response.json().catch(async()=>({error:await response.text()}));if(!response.ok)throw new Error(result.error||"Your response could not be saved.");setData((current:any)=>({...current,proof:{...current.proof,status:decision,customer_feedback:feedback}}));setMessage(decision==="approved"?"Proof approved. We will move your keepsake into production.":"Your requested changes were sent to Memento House.")}catch(error){setMessage((error as Error).message)}finally{setBusy(false)}}if(!data)return <main className="proofReviewPage"><p>{message||"Preparing your proof…"}</p>{message&&<a href="/account">← Return to your orders</a>}</main>;const final=["approved","changes_requested"].includes(data.proof.status);return <main className="proofReviewPage"><a href="/account">← Your orders</a><header><div className="eyebrow">Customer proof review</div><h1>{data.order.catalog_key||"Your Memento House keepsake"}</h1><p>Proof version {data.proof.version} · {data.proof.file_name}</p></header><section className="proofCanvas"><iframe title="Uploaded customer proof" src={data.url}/><a href={data.url} target="_blank" rel="noreferrer">Open proof full size ↗</a></section><section className="proofDecision"><h2>{final?"Your response":"Ready to approve?"}</h2>{data.proof.message&&<p>{data.proof.message}</p>}{!final&&<><label>Changes or notes<textarea value={feedback} onChange={event=>setFeedback(event.target.value)} placeholder="Describe any changes clearly, or leave blank if you approve the proof as shown."/></label><div className="buttonRow"><button className="button light" disabled={busy||!feedback.trim()} onClick={()=>respond("changes_requested")}>Request changes</button><button className="button gold" disabled={busy} onClick={()=>respond("approved")}>Approve this proof →</button></div></>}{final&&<p><b>{data.proof.status==="approved"?"Approved":"Changes requested"}</b>{feedback?` · ${feedback}`:""}</p>}{message&&<p role="status">{message}</p>}</section></main>}
