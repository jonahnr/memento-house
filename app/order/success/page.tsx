"use client";
import {useEffect,useState} from "react";
import {useSearchParams} from "next/navigation";
export default function Success(){const session=useSearchParams().get("session_id")||"",[data,setData]=useState<any>({status:"processing"});useEffect(()=>{let stopped=false,attempts=0;async function check(){const r=await fetch(`/api/order-status?session_id=${encodeURIComponent(session)}`),j=await r.json();if(stopped)return;setData(j);if(j.status==="processing"&&attempts++<15)setTimeout(check,1500)}if(session)check();else setData({status:"invalid"});return()=>{stopped=true}},[session]);return <main className="orderPage"><a href="/" className="houseBrand"><img src="/brand/memento-house-logo.webp" alt="Memento House"/><div>Memento House<small>Made for the moment. Kept for a lifetime.</small></div></a><section><div className="eyebrow">Payment confirmation</div>{data.status==="processing"?<><h1>We are confirming your order.</h1><p role="status">Stripe has returned you safely. Your account and next steps will appear here as soon as the verified payment webhook finishes.</p></>:data.status==="ready"?<><h1>{data.name} is confirmed.</h1><p>Your paid order is recorded. Reaching this page did not grant access; the verified Stripe event completed your setup.</p><ol className="successSteps">{data.nextSteps.map((step:string)=><li key={step}>{step}</li>)}</ol><div className="buttonRow"><a href={`/account/questionnaire?order=${data.orderId}`} className="button gold">Open my questionnaire →</a><a href="/dashboard" className="button light">Open dashboard</a></div></>:<><h1>We could not display this confirmation.</h1><p>Your payment may still be safe. Please check your receipt email or contact us with the email used at checkout.</p><a href="/contact" className="button gold">Contact Memento House →</a></>}</section></main>}
