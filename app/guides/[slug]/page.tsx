import type {Metadata} from "next";import {notFound} from "next/navigation";import {GUIDES,guideBySlug} from "../../../lib/guides";
export function generateStaticParams(){return GUIDES.map(guide=>({slug:guide.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,guide=guideBySlug(slug);if(!guide)return{};return{title:guide.title,description:guide.description,alternates:{canonical:`/guides/${guide.slug}`},openGraph:{title:guide.title,description:guide.description,type:"article",url:`/guides/${guide.slug}`}}}
export default async function Guide({params}:{params:Promise<{slug:string}>}){const{slug}=await params,guide=guideBySlug(slug);if(!guide)notFound();const schema={"@context":"https://schema.org","@type":"Article",headline:guide.title,description:guide.description,author:{"@type":"Organization",name:"Memento House"},publisher:{"@type":"Organization",name:"Memento House",logo:{"@type":"ImageObject",url:"https://mementohouse.com/brand/memento-house-logo.webp"}},mainEntityOfPage:`https://mementohouse.com/guides/${guide.slug}`};return <main className="guidePage"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><header><a href="/" className="houseBrand"><img src="/brand/memento-house-logo.webp" alt="Memento House"/><div>Memento House<small>Made for the moment. Kept for a lifetime.</small></div></a><a href="/guides" className="textLink">All guides</a></header><article><div className="eyebrow">Memento House guide</div><h1>{guide.title}</h1><p className="guideLead">{guide.description}</p>{guide.sections.map(([title,copy])=><section key={title}><h2>{title}</h2><p>{copy}</p></section>)}<aside><h2>Find the experience that fits.</h2><p>Explore the wedding collection or try each interactive customization experience before purchase.</p><a href="/#keepsakes" className="button gold">Explore the keepsakes →</a></aside></article></main>}
