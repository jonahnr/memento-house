import {MEMENTO_MAP_TYPES,MEMENTO_MAP_TYPE_IDS,type MementoMapType} from "../../lib/memento-map-types";
import {AccountLink} from "../account-link";
import {MapPricing} from "./map-pricing";
export function MementoMapTypeLanding({type}:{type:MementoMapType}){const config=MEMENTO_MAP_TYPES[type];return <main className="landing mapTypeLanding"><nav className="nav"><a href="/memento-map" className="brand"><img src="/brand/memento-house-logo.webp" alt="Memento House"/><span>Memento <i>Map</i><small>by Memento House</small></span></a><div className="navLinks"><a href="#how">How it works</a><AccountLink/><a href="#pricing" className="button small">Create your map</a></div></nav><section className="hero"><div className="eyebrow">{config.name} Memento Map</div><h1>{config.headline}</h1><p>{config.marketingDescription}</p><div className="heroActions"><a href="#pricing" className="button">Choose your package →</a><a href="/memento-map" className="button light">Explore every type</a></div><div className="trust"><span>{config.icon} {config.categoryDescriptions.slice(0,2).join(" • ")}</span><span>♡ Photos, videos, and stories</span><span>⌖ Real places on one shared map</span></div></section><section id="how" className="how"><div className="eyebrow">Made for this moment</div><h2>Bring the people and places together.<br/><em>Preserve what they mean.</em></h2><div className="steps">{config.how.map(([title,copy],index)=><article key={title}><b>{String(index+1).padStart(2,"0")}</b><h3>{title}</h3><p>{copy}</p></article>)}</div></section><section className="categorySection"><div><div className="eyebrow">What this map captures</div><h2>Every pin carries context.</h2><p>{config.memoryPrompt} Categories are initialized in the product and keep contributions easy to explore.</p></div><div className="layerPreview">{config.categories.slice(0,4).map(category=><span key={category}><i className="storyLayer"/>{category}</span>)}</div></section><section className="mapExamples"><div className="eyebrow">Picture the finished map</div><h2>Real contributions feel like this.</h2><div className="pricingGrid three">{config.examples.map(([title,detail])=><article className="priceCard" key={title}><span className="tag">{config.shortName}</span><h3>{title}</h3><p>{detail}</p></article>)}</div></section><section className="categorySection"><div><div className="eyebrow">Made to last</div><h2>{config.timelineLabel}</h2><p>{config.longevity}</p></div><div><p>{config.timelineIntro}</p></div></section><MapPricing type={type}/><section className="houseCta"><div className="eyebrow">Begin with the right experience</div><h2>{config.cta}.</h2><a href="#pricing" className="button gold">Choose a package →</a></section><section className="mapTypeDiscovery"><div className="eyebrow">More Memento Map experiences</div><h2>Every meaningful gathering has a geography.</h2><div>{MEMENTO_MAP_TYPE_IDS.filter(id=>id!==type).map(id=><a href={`/memento-map/${MEMENTO_MAP_TYPES[id].route}`} key={id}><span>{MEMENTO_MAP_TYPES[id].icon}</span><b>{MEMENTO_MAP_TYPES[id].name}</b><small>{MEMENTO_MAP_TYPES[id].description}</small></a>)}</div></section><footer><a href="/" className="brand"><img src="/brand/memento-house-logo.webp" alt="Memento House"/><span>Memento <i>Map</i></span></a><p>Made for the moment. Kept for a lifetime.</p></footer></main>}
