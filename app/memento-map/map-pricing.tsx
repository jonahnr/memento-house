import {mapComparison,mapTierPresentation,orderHref} from "../../lib/product-tiers";
import {mapTypeConfig,type MementoMapType} from "../../lib/memento-map-types";
export function MapPricing({type}:{type:MementoMapType}){const config=mapTypeConfig(type),tiers=mapTierPresentation(type),rows=mapComparison(type);return <><section className="pricingSection" id="pricing"><div className="eyebrow">{config.name} packages</div><h2>Choose how much of this story you want to preserve.</h2><div className="pricingGrid three">{tiers.map(tier=><article className={`priceCard ${tier.popular?"popular":""}`} key={tier.id}>{tier.popular&&<span className="popularTag">MOST POPULAR</span>}<h3>{tier.name}</h3><strong>${tier.price}</strong><p>{tier.subtitle}</p><ul>{tier.features.map(feature=><li key={feature}>✓ {feature}</li>)}</ul><a href={orderHref("map",tier.id,type)} className="button gold">{tier.cta} →</a></article>)}</div></section><section className="comparison"><div className="eyebrow">Feature comparison</div><h2>One platform, explained for this moment.</h2><div className="comparisonTable threePlans"><div><b>Feature</b><b>Map</b><b>Plus</b><b>Timeline Plus</b></div>{rows.map(row=><div key={row.label}><span>{row.label}</span>{row.values.map((value,index)=><span key={index}>{value}</span>)}</div>)}</div></section></>}
