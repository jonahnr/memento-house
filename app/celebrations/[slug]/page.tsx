import {notFound} from "next/navigation";
import Image from "next/image";
import {CELEBRATIONS,celebrationBySlug} from "../../../lib/celebrations";

export function generateStaticParams(){return CELEBRATIONS.map(item=>({slug:item.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const item=celebrationBySlug((await params).slug);return item?{title:`${item.name} Keepsakes — Memento House`,description:item.intro}:{} }

export default async function CelebrationPage({params}:{params:Promise<{slug:string}>}){const item=celebrationBySlug((await params).slug);if(!item)notFound();return <main className="celebrationExperiencePage"><nav><a href="/" className="houseBrand"><Image src="/brand/memento-house-logo.webp" alt="" width={72} height={72}/><span>Memento House<small>Made for the moment. Kept for a lifetime.</small></span></a><a href="/#celebrations">All celebrations</a></nav><header><div><span className="eyebrow">{item.status} · {item.name}</span><h1>{item.headline}</h1><p>{item.intro}</p><a className="button gold" href={item.status==="Available now"?"/#keepsakes":`/contact?celebration=${encodeURIComponent(item.name)}`}>{item.status==="Available now"?"Explore the collection →":"Join the early-access list →"}</a></div><Image src={`/brand/celebrations/${item.slug}-1.webp`} alt={`${item.name} Memento House celebration experience`} width={1200} height={1320} sizes="(max-width: 900px) 100vw, 48vw" priority/></header><section className="celebrationStory"><Image src={`/brand/celebrations/${item.slug}-2.webp`} alt={`Guests creating a ${item.name.toLowerCase()} keepsake together`} width={1200} height={1320} sizes="(max-width: 900px) 100vw, 48vw"/><div><span className="eyebrow">Made together</span><h2>The celebration becomes part of the keepsake.</h2><p>Each experience is designed to give guests a clear, meaningful way to participate. They add the words, places, stories, and messages that could only come from the people who were there.</p><ul>{item.moments.map(moment=><li key={moment}>{moment}</li>)}</ul></div></section><section className="celebrationProducts"><span className="eyebrow">Experiences for {item.name.toLowerCase()}</span><h2>Choose what you want everyone to help create.</h2><div>{item.products.map(product=><a key={product.name} href={product.href}><h3>{product.name}</h3><p>{product.description}</p><span>{item.status==="Available now"?"Explore this experience →":"Tell me when it launches →"}</span></a>)}</div></section><footer><a href="/contact">Questions or a custom celebration? Talk with us →</a></footer></main>}
