import {requireAdmin} from "../../../../lib/admin";
import {fulfillPurchase} from "../../../../lib/fulfillment";
import {resolveCatalog} from "../../../../lib/product-catalog";
export async function POST(request:Request){const context=await requireAdmin(request);if(!context)return new Response("Forbidden",{status:403});try{const body=await request.json(),product=String(body.product||""),tier=String(body.tier||""),addons=body.addons||String(body.addon||"").split(",").filter(x=>x&&x!=="none"),item=resolveCatalog(product,tier,addons),sourceId=`admin_${crypto.randomUUID()}`,result=await fulfillPurchase(context.admin,{source:"admin",sourceId,email:String(body.customerEmail||context.user.email).toLowerCase(),name:String(body.customerName||"Admin test customer"),userId:body.customerEmail?null:context.user.id,product,tier,addons,amount:item.price,currency:"usd",customizationId:null});return Response.json({ok:true,order:result.order,entitlement:item.entitlement,questionnaire:item.questionnaireType,workflow:item.fulfillmentWorkflow,nextSteps:item.nextSteps,redirect:`/order/test-success?order=${result.order.id}`})}catch(error){return new Response(error instanceof Error?error.message:"Simulation failed",{status:400})}}
