import {createClient} from "@supabase/supabase-js";
export async function POST(request:Request){const url=process.env.NEXT_PUBLIC_SUPABASE_URL||"https://kdcymeoldvwlmfwemfgq.supabase.co",key=process.env.SUPABASE_SERVICE_ROLE_KEY,token=(request.headers.get("authorization")||"").replace(/^Bearer\s+/i,"");if(!key||!token)return new Response("Unauthorized",{status:401});const admin=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}}),auth=await admin.auth.getUser(token),user=auth.data.user;if(!user)return new Response("Unauthorized",{status:401});const form=await request.formData(),file=form.get("file");if(!(file instanceof File)||!file.type.startsWith("image/")||file.size>8_000_000)return new Response("Choose an image smaller than 8 MB",{status:400});const bucket="memento-photos";await admin.storage.createBucket(bucket,{public:true,fileSizeLimit:8_000_000,allowedMimeTypes:["image/jpeg","image/png","image/webp"]});const ext=file.name.split(".").pop()?.toLowerCase()||"jpg",path=`${user.id}/${crypto.randomUUID()}.${ext}`,upload=await admin.storage.from(bucket).upload(path,file,{contentType:file.type,upsert:false});if(upload.error)return new Response(upload.error.message,{status:500});return Response.json({url:admin.storage.from(bucket).getPublicUrl(path).data.publicUrl})}
