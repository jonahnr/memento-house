import {createClient} from "@supabase/supabase-js";
import {resolveCatalog} from "../../../lib/product-catalog";
export async function GET(request:Request){const sessionId=new URL(request.url).searchParams.get("session_id")||"";if(!/^cs_[A-Za-z0-9_]+$/.test(sessionId))return Response.json({status:"invalid"},{status:400});const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return Response.json({status:"unavailable"},{status:503});const admin=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}}),result=await admin.from("orders").select("id,product,tier,order_status,questionnaire_status,customer_email").eq("stripe_session_id",sessionId).maybeSingle();if(result.error)return Response.json({status:"error"},{status:500});if(!result.data)return Response.json({status:"processing"});const item=resolveCatalog(result.data.product,result.data.tier);return Response.json({status:"ready",orderId:result.data.id,name:item.displayName,orderStatus:result.data.order_status,questionnaireType:item.questionnaireType,nextSteps:item.nextSteps,hasQuestionnaire:item.questionnaire.length>0})}
